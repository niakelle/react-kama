let initialState = {
	friendsData: [
		{ id: 1, name: "Dmytry" },
		{ id: 2, name: "Sasha" },
		{ id: 3, name: "Valery" },
	],
};

const navbarReducer = (state = initialState, action) => {


	return state;
}

export default navbarReducer;